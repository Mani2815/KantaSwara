/**
 * Prisma Client Proxy
 * Exports the Prisma Client initialized on the server directory
 * to ensure compatibility with imports pointing to @/lib/prisma.
 */
export { prisma } from '@server/lib/prisma';
